"use strict"

// .findIndex() method works almost the same way as .find(),
// but .findIndex() returns an index of found element and not the element itself
// To delete an element from an array, we use the .splice() method, but for the .splice() method
// we need the index at which we want to delete
